# Backend

Reference placeholder only.

For Personal Assistant V1, backend implementation should default to the shared Agent Enterprise server in `server/`, not a standalone per-module backend, unless the canonical docs in `../../` explicitly say otherwise.
